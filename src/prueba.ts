export {}

// const miPromise = new Promise<string | number>((resolve, reject) => {})
// const miPromise2 = new Promise<boolean | null>((resolve, reject) => {})
// const a = Promise<[]>

// const fnPromise = async <T>(promise: Promise<T>) => {

//     return [
//         promise,
//         'hello'
//     ]
// }

// const main = async () => {

//     const [promise, a] = await fnPromise(miPromise)
// }
